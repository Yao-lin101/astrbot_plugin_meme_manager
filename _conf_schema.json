{
  "image_host": {
    "description": "选择的图床",
    "type": "string",
    "hint": "请选择要使用的图床服务",
    "default": "stardots",
    "options": [
      "stardots",
      "cloudflare_r2"
    ]
  },
  "image_host_config": {
    "description": "图床配置",
    "type": "object",
    "items": {
      "stardots": {
        "description": "Stardots 图床配置",
        "type": "object",
        "items": {
          "key": {
            "description": "API Key",
            "type": "string",
            "hint": "请输入 Stardots 的 API Key"
          },
          "secret": {
            "description": "API Secret",
            "type": "string",
            "hint": "请输入 Stardots 的 API Secret"
          },
          "space": {
            "description": "空间名称",
            "type": "string",
            "hint": "请输入 Stardots 的空间名称"
          }
        }
      },
      "cloudflare_r2": {
        "description": "Cloudflare R2 图床配置",
        "type": "object",
        "items": {
          "account_id": {
            "description": "Cloudflare Account ID",
            "type": "string",
            "hint": "请输入 Cloudflare Account ID"
          },
          "access_key_id": {
            "description": "R2 Access Key ID",
            "type": "string",
            "hint": "请输入 R2 Access Key ID"
          },
          "secret_access_key": {
            "description": "R2 Secret Access Key",
            "type": "string",
            "hint": "请输入 R2 Secret Access Key"
          },
          "bucket_name": {
            "description": "R2 Bucket 名称",
            "type": "string",
            "hint": "请输入 R2 Bucket 名称"
          },
          "public_url": {
            "description": "自定义CDN域名（可选）",
            "type": "string",
            "hint": "自定义CDN域名，例如：https://cdn.yourdomain.com"
          }
        }
      }
    }
  },
  "webui_port": {
    "description": "Web UI 端口号",
    "type": "int",
    "default": 5000
  },
  "meme_prompt": {
    "description": "表情包系统提示词",
    "type": "text",
    "hint": "自动注入到人格提示词尾部，用于指导大模型在回复中使用 &&表情标记&&。支持自定义修改。",
    "default": "【表情包发送规则】：你在每次回复中都应该自然地在合适位置加入表情标记来配合你的语气 and 情绪。表情需用&&包裹，格式如 &&情感词&&（例如 &&开心&& 或 &&miku&&）。\n规则：\n1. 在日常对话中，你应该主动且频繁地使用表情标记，每次回复至少使用1个表情标记。\n2. 表情标记应放在回复文本的自然位置（句首、句中或句末均可），用来表达你当前的情绪、动作或反应。\n3. 标记词可以是任何描述情绪、动作、表情或状态 of 词语（如 &&得意&&、&&害羞&&、&&摸头&&、&&卖萌&& 等），系统会自动通过语义匹配找到最合适的表情包。\n4. 仅在严肃的专业咨询（医疗/法律等）场景下可以不使用表情。\n\n【绝对禁止使用搜索/发图工具】：当用户要求你发送表情包、图片、或提及\"发个...表情包/图片\"时，**严禁使用任何外部搜索工具（如 web_search、tavily 等）去网络搜索图片，也严禁调用任何第三方发图或消息发送工具。你只需直接在回复文本中输出对应的 &&标签&& 标记，系统会自动在后台拦截此标签并从本地匹配发送表情包。你调用任何搜索或发图工具都是错误且被禁止的！**"
  },
  "interaction_config": {
    "description": "表情包交互配置",
    "type": "object",
    "items": {
      "max_emotions_per_message": {
        "description": "每次回复最多使用表情数量",
        "type": "int",
        "default": 2,
        "hint": "每次回复最多使用表情数量"
      },
      "emotions_probability": {
        "description": "表情出现概率",
        "type": "int",
        "default": 50,
        "hint": "每次发送表情出现的概率，范围为1-100",
        "slider": {
          "min": 1,
          "max": 100,
          "step": 10
        }
      },
      "enable_alternative_markup": {
        "description": "启用备用标记",
        "type": "bool",
        "default": true,
        "hint": "如果为true，则会尝试使用备用标记, 例如: &&happy&& 和 [happy] / (happy) 是等价的，防止大模型自己编标记，一般情况建议开启"
      },
      "enable_mixed_message": {
        "description": "启用回复带图",
        "type": "bool",
        "default": true,
        "hint": "文本和表情图片在同一条消息中发送（注意：分段回复可能导致功能失效）"
      },
      "mixed_message_probability": {
        "description": "回复带图概率",
        "type": "int",
        "default": 50,
        "hint": "启用回复带图时的发送概率，范围为1-100",
        "slider": {
          "min": 1,
          "max": 100,
          "step": 10
        }
      },
      "convert_static_to_gif": {
        "description": "静态图转GIF发送",
        "type": "bool",
        "default": false,
        "hint": "开启后，发送静态图片表情时会将其转换为1帧的GIF格式"
      },
      "streaming_compatibility": {
        "description": "流式传输兼容",
        "type": "bool",
        "default": false,
        "hint": "开启后，在流式传输模式下表情图片将作为独立消息发送（流式模式下无法混合图文）"
      }
    }
  },
  "auto_steal_config": {
    "description": "自动偷表情包配置",
    "type": "object",
    "items": {
      "auto_steal_enabled": {
        "description": "启用暗中自动偷表情包",
        "type": "bool",
        "default": false,
        "hint": "开启后，若群聊消息中的图片命中概率，将暗中执行偷表情包行为（需要多模态模型以自动判定偏好并分类）"
      },
      "auto_steal_probability": {
        "description": "自动偷表情包概率",
        "type": "int",
        "default": 30,
        "hint": "自动偷表情包触发的概率，范围为1-100",
        "slider": {
          "min": 1,
          "max": 100,
          "step": 1
        }
      },
      "auto_steal_min_seen": {
        "description": "自动偷表情包最少出现次数",
        "type": "int",
        "default": 2,
        "hint": "图片在群聊中总计被看见达到多少次后，才触发多模态模型偷表情判定。设为 1 表示首次看到就判定，设为 2 可有效过滤一次性截图或插画"
      }
    }
  },
  "multimodal_config": {
    "description": "多模态分类配置",
    "type": "object",
    "items": {
      "multimodal_llm_enabled": {
        "description": "启用多模态模型自动分类表情包",
        "type": "bool",
        "default": false,
        "hint": "开启后，偷取表情包时将自动调用指定的多模态模型对图片进行视觉分析并分类"
      },
      "multimodal_llm_provider_id": {
        "description": "多模态模型Provider ID",
        "type": "string",
        "default": "",
        "hint": "留空则使用当前对话的模型（当前对话模型需支持图片理解）",
        "_special": "select_provider"
      }
    }
  },
  "compression_config": {
    "description": "表情包压缩配置",
    "type": "object",
    "items": {
      "enable_compression": {
        "description": "启用自动压缩表情包",
        "type": "bool",
        "default": true,
        "hint": "上传或偷取表情包时，如果文件超出大小或分辨率限制，将自动进行压缩。"
      },
      "compression_max_size_kb": {
        "description": "表情包最大大小 (KB)",
        "type": "int",
        "default": 1024,
        "hint": "超出该大小的文件将被压缩"
      },
      "compression_max_width": {
        "description": "表情包最大尺寸 (像素)",
        "type": "int",
        "default": 1024,
        "hint": "图片宽度若超出此像素值，将等比例缩小"
      },
      "compression_quality": {
        "description": "压缩质量 (1-100)",
        "type": "int",
        "default": 80,
        "hint": "压缩静态图时所采用的质量比例，值越低图片越小但质量越差"
      },
      "compress_gif": {
        "description": "启用GIF格式压缩",
        "type": "bool",
        "default": false,
        "hint": "开启后将尝试压缩动图GIF（由于可能损坏动画帧，建议关闭）"
      },
      "compression_format": {
        "description": "表情包压缩格式",
        "type": "string",
        "default": "original",
        "options": [
          "original",
          "webp",
          "avif"
        ],
        "hint": "选择压缩后的图片保存格式。建议选择 webp 以兼顾体积和聊天客户端兼容性；original 保持原格式；avif 拥有最大压缩率（动图仍为 gif）"
      }
    }
  },
  "embedding_config": {
    "description": "向量检索配置",
    "type": "object",
    "items": {
      "embedding_provider_id": {
        "description": "Embedding 模型 Provider ID",
        "type": "string",
        "default": "",
        "hint": "请选择当前已启用的 Embedding 提供商。留空则默认使用系统第一个可用的 Embedding 节点。",
        "_special": "select_embedding_provider"
      },
      "embedding_similarity_threshold": {
        "description": "向量相似度最低阈值 (0-1)",
        "type": "float",
        "default": 0.6,
        "hint": "Cosine 相似度大于此阈值时才允许召回。若相似度较低，则忽略该表情。"
      },
      "embedding_tag_weight": {
        "description": "LLM 显式输出表情词匹配权重",
        "type": "float",
        "default": 0.7,
        "hint": "LLM 显式输出情感词标记（如 &&高兴&&）的相似度加权比例。"
      },
      "embedding_text_weight": {
        "description": "LLM 回复文本内容匹配权重",
        "type": "float",
        "default": 0.3,
        "hint": "LLM 回复文本内容语义的相似度加权比例。双通道混合检索总分 = 显式标签相似度 * 标签权重 + 回复文本相似度 * 文本权重。"
      }
    }
  }
}